# Debugging Analysis

## CheckIfCookieExists 1 & 2

The first screenshot shows that the cookie exists, the second shows that when a cookie exists the function returns true. The boolean returned from this function is used to validate if the username cookie exists.

## InitializesSession 1 & 2

The first screenshot shows the input username visible before its checked that the cookie exists via the checkForUserCookie function it shows how the correct buttons are set to be hidden when it exists.

## InitializesSession 3 & 4

This screenshot shows the opposite of the other two, it shows that when the cookie does not exist the the newPlayerButton is hidden and the input is set to be unhidden so new users can enter their name.